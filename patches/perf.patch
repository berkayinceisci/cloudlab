diff --git i/tools/perf/builtin-stat.c w/tools/perf/builtin-stat.c
index ab40d85fb..ef6fdafa0 100644
--- i/tools/perf/builtin-stat.c
+++ w/tools/perf/builtin-stat.c
@@ -2696,8 +2696,14 @@ int cmd_stat(int argc, const char **argv)
 			return -1;
 		}
 		if (!stat_config.json_output) {
+			struct tm lt;
+			char dt[64];
+
 			clock_gettime(CLOCK_REALTIME, &tm);
-			fprintf(output, "# started on %s\n", ctime(&tm.tv_sec));
+			localtime_r(&tm.tv_sec, &lt);
+			strftime(dt, sizeof(dt), "%a %b %e %H:%M:%S", &lt);
+			fprintf(output, "# started on %s.%09ld %d\n",
+				dt, tm.tv_nsec, 1900 + lt.tm_year);
 		}
 	} else if (output_fd > 0) {
 		mode = append_file ? "a" : "w";
diff --git i/tools/perf/pmu-events/arch/x86/skylakex/uncore-cache.json w/tools/perf/pmu-events/arch/x86/skylakex/uncore-cache.json
index da46a3aeb..f6043e909 100644
--- i/tools/perf/pmu-events/arch/x86/skylakex/uncore-cache.json
+++ w/tools/perf/pmu-events/arch/x86/skylakex/uncore-cache.json
@@ -4850,6 +4850,16 @@
         "UMask": "0x31",
         "Unit": "CHA"
     },
+    {
+        "BriefDescription": "TOR Cycles; All from Local iA",
+        "Counter": "0,1,2,3",
+        "EventCode": "0x1f",
+        "EventName": "UNC_CHA_TOR_CYCLES.IA",
+        "PerPkg": "1",
+        "PublicDescription": "Increments by one each cycle if there are any entries in the TOR",
+        "UMask": "0x31",
+        "Unit": "CHA"
+    },
     {
         "BriefDescription": "TOR Occupancy; Hits from Local iA",
         "Counter": "0",
@@ -4934,6 +4944,16 @@
         "UMask": "0x21",
         "Unit": "CHA"
     },
+    {
+        "BriefDescription": "TOR Cycles; Misses from Local iA",
+        "Counter": "0,1,2,3",
+        "EventCode": "0x1f",
+        "EventName": "UNC_CHA_TOR_CYCLES.IA_MISS",
+        "PerPkg": "1",
+        "PublicDescription": "For each cycle, this event accumulates the number of valid entries in the TOR that match qualifications specified by the subevent.   T",
+        "UMask": "0x21",
+        "Unit": "CHA"
+    },
     {
         "BriefDescription": "TOR Occupancy : CRds issued by iA Cores that Missed the LLC",
         "Counter": "0",
